# Taste
- Communicates in Indonesian (Bahasa Indonesia); conversations should be conducted in Indonesian. Confidence: 0.9
- Prefers to follow the reference implementation (9router) exactly rather than a documented-but-different official approach — e.g., matching `/v1/models` response shape (omitting `created`, alias-prefixed ids), and when adding the Command Code provider choosing 9router's actual integration (alpha/generate endpoint + NDJSON protocol + static model catalog) over the official Provider API ("ikutin dari 9router tadi"). Confidence: 0.95
- Prefers all provider credentials (apiKey/access_token) to be stored in the database and loaded at runtime per provider, rather than env vars or hardcoded config — "semua apikey atau access_token itu semuanya nanti di input di database, nanti tinggal di pake sesuai provider" (e.g., OpenAI/Codex uses its access token, other providers follow existing patterns). Confidence: 0.9
- Prefers consistent credential handling across all provider executors (each accepting both `apiKey` and `accessToken` and each having a token-import endpoint), rather than per-provider special cases. Confidence: 0.7
- Prefers a tidy, modular monorepo with clear separation of concerns: split pure format-translation logic into a dedicated package (`@srouter/translator`) and provider HTTP/transport classes into another (`@srouter/executors`), instead of mixing translation + transport inside one package — requested "biar lebih rapih aja dan enak", mirroring 9router's `open-sse/translator` + `open-sse/executors` split. Confidence: 0.9
- When building a feature, prefers porting the reference project's (9router) exact implementation and data structures over inventing a new design — e.g., porting `open-sse/providers/pricing.js` (MODEL_PRICING, `getPricingForModel`, `calculateCostFromTokens`, `formatCost`) as a new `@srouter/pricing` package, and matching 9router's usage stats shape (`totalRequests`, `totalInputTokens`, `totalOutputTokens`, `byModel`) for the usage/cost feature. Confidence: 0.7
- Prefers keeping configuration/data catalogs scoped strictly to what actually exists and is used in the project, not shipping full reference ports with unused entries — e.g., when asked to fix the pricing, narrowed `MODEL_PRICING` to only the models actually registered in SRouter (60: commandcode + antigravity + anthropic fallback) and removed 9router's `PROVIDER_PRICING`/`PATTERN_PRICING`/`matchPattern` since SRouter doesn't use tokenrouter ("coba pricingnya yang model yang ada dulu jangan ada yang lain dulu"). Confidence: 0.9
- Prefers keeping usage analytics backward-compatible when extending API shapes: add new fields (e.g. 9router-style `totalInputTokens`, `estimatedCost`, `costLabel`, `byModel`) while retaining existing field names as aliases so nothing breaks. Confidence: 0.7
- Prefers cost/usage values clearly marked as estimates ("Est. Cost ~ Estimated, not actual billing"), including an `estimated: true` flag and `formatCost` `$X.XX` label, rather than presenting estimated costs as real billing. Confidence: 0.8
- When splitting transport classes out of `@srouter/providers`, prefers renaming them `*Executor` (OpenAIExecutor, CommandCodeExecutor, etc.) and having `apps/api` import directly from `@srouter/executors` (no cross re-export/facade from `@srouter/providers`) — one entry point per responsibility. Confidence: 0.7
- Prefers committing changes as separate, logically-grouped commits (per category/package: types, translator+pricing, executors+providers, api) rather than one large "commit everything" — "gas commit tapi gak disemuanya ... mending percategory aja", and keeps unrelated/tool-generated files (e.g. `.commandcode/` taste settings) out of feature commits. Confidence: 0.9
- Prefers commit messages WITHOUT the `Co-authored-by: CommandCodeBot <noreply@commandcode.ai>` trailer — explicitly requested "jangan ada Co-authored-by: CommandCodeBot <noreply@commandcode.ai> tolong". Confidence: 0.95
- Prefers lightweight frontend stacks over heavier frameworks: when offered Next.js 15 (App Router) as the main recommendation vs Vite + React + TanStack Router as the lighter alternative, chose "Vite + React + TanStack Router aja" for a new dashboard app in the monorepo. Confidence: 0.7
