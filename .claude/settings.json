{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "permissions": {
        "deny": ["Bash(pnpm run *)", "Bash(pnpm dev *)", "Bash(pnpm build *)"]
    },
    "hooks": {
        "PreToolUse": [
            {
                "matcher": "Bash",
                "hooks": [
                    {
                        "type": "command",
                        "command": "python3 -c 'import json,shlex,sys; d=json.load(sys.stdin); c=d.get(\"tool_input\",{}).get(\"command\",\"\"); t=shlex.split(c); blocked=any(x==\"pnpm\" and any(y in {\"run\",\"dev\",\"build\"} for y in t[i+1:]) for i,x in enumerate(t)); print(json.dumps({\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Blocked: this repository forbids pnpm run/dev/build commands.\"}} if blocked else {}))'",
                        "statusMessage": "Blocking prohibited pnpm commands"
                    }
                ]
            }
        ]
    }
}
