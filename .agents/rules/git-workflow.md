# Rule: Git Commit and Push Workflow

- NEVER run `git commit` or `git push` automatically.
- Only run `git commit` or `git push` when the user explicitly requests or instructs you to do so in their prompt.
